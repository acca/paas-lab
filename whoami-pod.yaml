kind: Pod
apiVersion: extensions/v1beta1
metadata:
  name: whoami
  namespace: default
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: whoami
    spec:
      containers:
      - name: whoami
        image: jwilder/whoami
